---
description: Generate a print-ready A4 one-pager / proposal / flyer
---

# /design-onepager <brief>

1. Treat the argument as the document brief (purpose, audience, the one
   message it must land). If empty, ask for one.
2. Follow the `design-onepager` skill
   (`.agents/skills/design-onepager/SKILL.md`), including the A4 fit
   check.
3. Save to `output/onepagers/YYYY-MM-DD-<slug>.html`.
4. Report the path and offer `/design-export` for the A4 PDF.
